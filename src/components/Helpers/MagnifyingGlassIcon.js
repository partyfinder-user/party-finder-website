import React from "react";

const MagnifyingGlassIcon = React.forwardRef(function MagnifyingGlassIcon(
  props,
  ref
) {
  return <svg ref={ref} {...props}></svg>;
});

export default MagnifyingGlassIcon;
